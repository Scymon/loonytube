import ThreadsShell from "@/components/threads/ThreadsShell";

export default function ThreadsLayout({ children }: { children: React.ReactNode }) {
  return <ThreadsShell>{children}</ThreadsShell>;
}
